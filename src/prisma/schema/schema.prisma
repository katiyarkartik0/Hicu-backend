generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // url      = env("PG_SQL_DATABASE_URL")
  url = "postgresql://newtonschool:dummy@localhost:5432/stealth?schema=public"
}

enum MemberStatus {
  INVITED
  ACTIVE
  REMOVED
}
